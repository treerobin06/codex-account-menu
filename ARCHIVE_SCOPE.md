# 公开包范围

仓库包含原生菜单栏 App、CLI、本机 relay、可选的 Copilot 上游适配、部署示例、合成测试与使用文档。

不包含账号、密钥、SSH 配置、对话历史、运行数据库、恢复票据、真实网络拓扑、内部项目档案或构建缓存。上游源码按固定版本单独提供；依赖与客户端二进制不随源码分发。

App 附件从当前公开源码构建，包含上游许可文本。当前为 Apple Silicon / ad-hoc 签名产物，未公证。运行依赖与客户端兼容边界见 [README](README.md)。

所有安装和服务器示例都需要用户自己的配置；示例地址、账号名称和 `local` 占位值不提供任何实际访问权限。普通 ChatGPT 账号切换不依赖服务器示例。
