# 隐私与数据边界

本工具在本机保存账号与配置状态，没有自建的账户云同步服务，也不向项目作者上传登录或对话。

## 本地敏感数据

保存的 OAuth 登录副本、账号索引、额度缓存、恢复副本及原客户端的对话均属于用户私有数据。它们位于当前用户的数据目录，不包含在源码或发布附件中。不要将这些文件附在公开 Issue；报告问题时保留错误类型、版本和复现步骤即可。

本机目录使用私有权限，并把账号库绑定到一个 Codex home。文件权限不等于加密存储；拥有该用户权限的进程仍可能访问其文件。

## 网络请求

- 官方账号与额度查询访问对应官方服务。
- Copilot 模型请求发往用户自行配置的代理；它可能消耗用户的上游额度。
- relay 重建代理授权头，剥离官方身份凭据，拒绝跨目标重定向或意外回退。
- 可选的 SSH 额度采集在远端读取用户授权，只返回白名单状态字段。

relay 仅监听 loopback，但同一台机器上的其他进程仍属于需要信任的本地边界。

## 对话预览与活动记录

对话预览按需读取既有 JSONL 文件的有限原始字节，再筛选用户/助手文字。读取片段可能包含其他条目；隐藏推理、系统指令和工具正文不会展示，也不会由预览功能另存或上传。它不复制整个历史。传输侧仅在内存中保留有上限的时间、状态和任务标识，不记录 Prompt、回答或凭据；helper 重启会清空这些记录。

## 公开发布范围

公开仓库提供通用代码、示例和合成测试。个人用户名、主机地址、部署账号、真实任务记录及内部运维报告不作为示例分发。网络示例使用文档保留地址或 `.invalid` 域名，需要用户自行填写。

发现发布内容中仍有个人信息时，请报告文件路径和行号，不要在 Issue 中复制完整秘密。源码删除或授权文件备份都不能撤销已经发出的远端令牌；凭据撤销与轮换应通过对应官方渠道完成。
